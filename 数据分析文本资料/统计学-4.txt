二分类模型评估指标

1.决策类 --正确率，召回率，精确度，F1分数

2.排序类-- ROC指标，GINI指数， K-S统计量， 提升度

模型的内部有效和外部有效

一个模型在train数据集中越复杂，偏差越小（偏差小模型越准确），那么在测试数据集中方差大（方差越小模型越稳定）。
调整模型的超参数，使得模型由简单到复杂，根据模型在测试数据集上的表现，确定最合适的超参数。

C的增加，回归系数的值逐一的归于0

当C很大时，惩罚项的权重很小，模型复杂度越大，稳定性越差，精确度越高。
